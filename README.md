# module-ballerinax-cdc.schema.aws.s3.driver
This library provides the drivers needed to use AWS S3 as an internal schema history storage mechanism for the Ballerina CDC (Change Data Capture) connector.
